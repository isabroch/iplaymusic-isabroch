const carousel = new Flickity( '.carousel__track', {
  // options
  cellAlign: 'center',
  contain: true,
  // percentPosition: false,
  wrapAround: true,
  imagesLoaded: true,
  prevNextButtons: false,
  pageDots: false,
});